First run generate_normal.py to generate the upper and lower bound for different metrics and instances. To evaluate the performance of anomaly detection, create a json file called observation_period.json and add the observation period for each metric in the json file. Then run prome_metrics.py to generate the csv that contains the metric data from Prometheus and evaluate the anomaly detection.