First run generate_normal.py to generate the upper and lower bound for different metrics and instances. Then run generate_csv.py to generate the css that contains the metric data from Prometheus. To evaluate the anomaly detection, create a folder call observation period, and create a json file called {metric}.json. Write the observation period in the json file. Then, run prome_metrics.py to evaluate the anomaly detection.